import makeStyles from '@mui/styles/makeStyles';

const style = makeStyles((theme) => ({    
    container: {
        maxWidth: 1920,
        float: 'none',
        margin: 'auto',
        width: '100%',
        padding: '0 15px',
    },
}));

export default style;